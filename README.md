# BULD-A-DECISION_TREE_MODEL
This Jupyter Notebook guides users through building and evaluating a Decision Tree classifier on airline survey data (129k+ records). It handles data preprocessing (encoding, missing values, train/test split), trains a model to predict customer satisfaction, evaluates it (~93.5% accuracy), and visualizes the tree structure.
